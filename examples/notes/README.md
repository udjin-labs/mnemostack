# Put your notes here

Any `*.md` or `*.txt` files in this directory are indexed by the
`mnemostack` container as `/data`. Drop your own markdown in here before
running `mnemostack index /data ...` from the compose example.

This file is just a placeholder so the directory exists in the repo.
